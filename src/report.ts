import { from } from 'rxjs';
import { Survey } from  './survey';

export class Report{
    constructor(initialChoices: Number[], initialChoiceTimes: Number[],
                swapChoices: Boolean[], swapToChoices: Number[], swapChoiceTimes: Number[],
                didWin: Boolean[], survey: Survey) {

    }
}